/**
 * The WCAG version and level the dashboard names.
 *
 * One constant, because the project has an open decision here (docs/project-plan.md, D1):
 * the brief and mockup say WCAG 2.1 AA, the repository says 2.2 AA. Whichever is chosen,
 * this is the only line in the dashboard to change.
 */
export const WCAG_TARGET = 'WCAG 2.2 AA';
